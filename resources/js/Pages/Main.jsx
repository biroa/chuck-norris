import React from "react";

const Main = () => {
    const react = "React";
    const inertia = "Inertia";
    const laravel = "Laravel";

    return (
        <h1>
            {react} + {inertia} + {laravel}
        </h1>
    );
};

export default Main;
